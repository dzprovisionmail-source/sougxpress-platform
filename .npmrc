# Forcer le linker hoisted pour la compatibilité avec Expo et React Native dans un monorepo pnpm.
# Cela résout les problèmes de résolution des modules natifs comme expo-clipboard.
node-linker=hoisted
